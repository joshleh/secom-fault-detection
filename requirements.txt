# ── Runtime dependencies (used by Streamlit Cloud + Docker API image) ──
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2
joblib==1.4.2
plotly==5.24.1
shap==0.46.0
streamlit==1.39.0

# ── API runtime ──
# Bundled here (not in requirements-dev) so the Docker image, which only
# installs requirements.txt, has uvicorn on PATH for the entrypoint.
fastapi==0.115.0
uvicorn[standard]==0.30.6
